/**
 * Graphics and GWC Library for Cadex OS
 * 
 * This contains some C++ bindings for TrueGL Graphics Components
 * The source for the graphics components can be found on lib folder
*/

#include <stdio.h>
#include <stdlib.h>
#include <stddef.h>

bool isWindowCompositorRunning();

namespace GWC
{
    // lib/widgets/WidgetBase.cpp
    class WidgetBase
    {
    private:
        /* data */
    public:
        WidgetBase(/* args */);
        ~WidgetBase();
    };
    
    // lib/widgets/GButton.cpp
    class GButton : WidgetBase
    {
    private:
        /* data */
    public:
        GButton();
        ~GButton();
    };

    // lib/widgets/GTextEdit.cpp
    class GTextEdit : WidgetBase
    {
    private:
        /* data */
    public:
        GTextEdit(/* args */);
        ~GTextEdit();
    };
    

}