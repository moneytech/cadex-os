#ifndef _FSTREAM
#define _FSTREAM

#include <iostream>
#include <string>

namespace std {
class File {
    private:
    public:
    auto Write(std::String *path, std::String *contents) -> void;
};
} // namespace std
#endif // !_FSTREAM
